apiVersion: v1
name: cinder-csi
# This version is never actually shipped. istio/release-builder will replace it at build-time
# with the appropriate version
version: 1.0.0
appVersion: 2.1.0
description: Helm chart for deploying Cinder-CSI
engine: gotpl
